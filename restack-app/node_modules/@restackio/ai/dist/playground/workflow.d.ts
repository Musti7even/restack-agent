type PlaygroundInput = {
    functionName: string;
    taskQueue: string;
    input: any;
};
type PlaygroundOutput = any;
export declare function playgroundRun({ functionName, taskQueue, input, }: PlaygroundInput): Promise<PlaygroundOutput>;
export {};
